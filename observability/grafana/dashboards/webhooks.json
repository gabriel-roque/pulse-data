{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {"type": "timeseries", "title": "Webhook delivery attempts", "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}, "targets": [{"expr": "sum by (result) (rate(pulse_webhook_delivery_total[5m]))", "legendFormat": "{{result}}"}]},
    {"type": "timeseries", "title": "Webhook retries", "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}, "targets": [{"expr": "sum(rate(pulse_webhook_retry_total[5m]))", "legendFormat": "retries/s"}]},
    {"type": "stat", "title": "Webhook DLQ", "gridPos": {"h": 5, "w": 12, "x": 0, "y": 8}, "targets": [{"expr": "sum(increase(pulse_webhook_dlq_total[1h]))"}]},
    {"type": "timeseries", "title": "Processing latency", "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8}, "targets": [{"expr": "histogram_quantile(0.95, sum by (le, worker) (rate(pulse_processing_latency_seconds_bucket[5m])))", "legendFormat": "{{worker}} p95"}]}
  ],
  "schemaVersion": 39,
  "tags": ["pulse", "webhooks"],
  "time": {"from": "now-6h", "to": "now"},
  "title": "Pulse Webhooks",
  "uid": "pulse-webhooks",
  "version": 1
}
